
<%= form_for @blog, html: {multipart: true} do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>
	
	<%= f.label :body %>
	<%= f.text_area :body, id: "wysiwyg" %>
	
	<%= f.submit :submit %>
<% end %>